version: 0.0
os: linux

files:
  - source: /
    destination: /var/www/html/hoppscotch/
file_exists_behavior: OVERWRITE

permissions:
  - object: /var/www/hoppscotch
    pattern: "**"
    owner: ubuntu
    group: ubuntu
    mode: 755
